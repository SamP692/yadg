console.log('game js loaded');

class GameList {
  constructor(teamList, numOfTeams) {
    this.teams = teamList;
    this.numOfTeams = numOfTeams;
    this.gamesCreated = 0;
  }
  createGames() {
    while (this.gamesCreated < (this.numOfTeams / 2)) {
      let x = stuff;
    }
  }
  renderGame() {
  }
}
